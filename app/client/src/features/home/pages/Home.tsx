import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Button variant={"ghost"} size={"sm"}>
        Cool Button
      </Button>
    </div>
  );
};

export default Home;
